gnome-terminal  --tab -e "bash -c 'roscore; bash'" --tab -e "bash -c 'roslaunch motoman_moveit sia5_dual_arm_stepper_azure_bringup.launch; bash'"
gnome-terminal  --tab -e "bash -c '/home/hayashi/.ros_startup_bash; bash'" --tab -e "bash -c 'roslaunch motoman_moveit sia5_dual_arm_stepper_azure_bringup.launch; bash'"
gnome-terminal  --tab -e "bash -c '/home/hayashi/.ros_startup_bash; bash'"

gnome-terminal  --tab -e "bash -c 'source /opt/ros/noetic/setup.bash; roscore; bash'" --tab -e "bash -c 'source /opt/ros/noetic/setup.bash; roslaunch motoman_moveit sia5_dual_arm_stepper_azure_bringup.launch; bash'"

roslaunch motoman_moveit sia5_dual_arm_stepper_azure_bringup.launch

gnome-terminal  --tab -e "bash -c 'roscore'" --tab -e "bash -c 'roslaunch '" --tab -e "bash -c 'top -n 1; bash'"
gnome-terminal  --tab -e "bash -c 'ps -ef; bash'" --tab -e "bash -c 'ls; bash'" --tab -e "bash -c 'top -n 1; bash'"


sudo apt-get install xdotool
xdotool key ctrl+shift+t


================================================================================
steps to install
1. create the 1 + no. of roslaunch bash script files
	1 is bash script for the system bringup
	the rest is the roscore, rosrun, and roslaunch...
	
2. use startup application
	Add new startup service
	Put the command to run the system bringup in the edit line, save and try restart

================================================================================

gnome-terminal -e "bash -c '/home/hayashi/.system_bringup; bash'"

gnome-terminal \
  --tab -e "bash -c '/home/hayashi/.ros_startup_bash; bash'" \
  --tab -e "bash -c 'sleep 2; /home/hayashi/.sia5_bringup; bash'" \


================================================================================